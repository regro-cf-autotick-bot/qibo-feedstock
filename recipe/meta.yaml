{% set name = "qibo" %}
{% set version = "0.1.8" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/qiboteam/qibo/archive/v{{ version }}.tar.gz
  sha256: ccedca36d2d1065cc4ee44af75db3c662f923813f6a84ed2289ebfd8538428f6

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - cma
    - joblib
    - matplotlib-base
    - psutil
    - python >=3.7
    - pyyaml
    - scipy
    - sympy
    - importlib_metadata
    - tabulate

test:
  imports:
    - qibo
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/qiboteam/qibo
  summary: A framework for quantum computing with hardware acceleration.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - scarrazza
