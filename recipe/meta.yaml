{% set name = "qibo" %}
{% set version = "0.1.10" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/qiboteam/qibo/archive/v{{ version }}.tar.gz
  sha256: 7391511366671f41eed3e97a60670b8c3022f6469e488eb974118fe4f402c8d4

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - cma
    - joblib
    - matplotlib-base
    - psutil
    - python >=3.7
    - pyyaml
    - scipy
    - sympy
    - importlib_metadata
    - tabulate

test:
  imports:
    - qibo
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/qiboteam/qibo
  summary: A framework for quantum computing with hardware acceleration.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - scarrazza
